You are an evaluation judge.

Your job is to evaluate every markdown summary file in the current working directory using the same rubric.

Rules:
- Evaluate only the input files provided below.
- Do not inspect parent directories, sibling directories, hidden metadata, or any previous judgment outputs.
- Do not use tools to inspect files. All required file contents are included below.
- Use only the provided `.txt` source/reference content and the provided `.md` summaries to score.
- Return exactly one JSON object on stdout and nothing else.
- Do not write any files.
- The JSON must be valid UTF-8 and pretty-printed.
- Output only the rubric data. Do not include prefaces, explanations, Markdown fences, commentary, or extra keys.
- Use exactly the top-level keys and nested keys shown below.
- Every summary filename must appear exactly once in `results` and exactly once in `ranking`.
- Every filename must be copied exactly from this allowed list, including the `.md` extension:
$allowed_filenames
- `ranking` must assign unique ranks starting at 1 with no gaps.
- `scores` must contain exactly these rubric keys: `accuracy`, `completeness`, `clarity`, `faithfulness_to_source_prompt`, `actionability`.
- Every score must be an integer from 0 to 5.
- `overall_score` must be a number from 0 to 5.

Context:
- Judge name: $judge_name
- Review set directory name: $set_name

Required JSON shape:
{
  "judge_name": "$judge_name",
  "set_name": "$set_name",
  "evaluated_at": "ISO-8601 timestamp if your CLI can provide one, otherwise leave as null",
  "source_text_file": "name of the .txt file you used",
  "rubric_version": "string",
  "results": [
    {
      "filename": "summary filename",
      "scores": {
        "accuracy": 0,
        "completeness": 0,
        "clarity": 0,
        "faithfulness_to_source_prompt": 0,
        "actionability": 0
      },
      "overall_score": 0,
      "strengths": ["..."],
      "weaknesses": ["..."],
      "notes": "..."
    }
  ],
  "ranking": [
    {
      "filename": "summary filename",
      "rank": 1
    }
  ],
  "overall_notes": "..."
}

Rubric:
$rubric_text

Input Files:
$input_bundle
